# ensemble — environment variables
#
# Copy to .env and fill in values before running locally.
# For Modal runs, no env vars needed here — params are resolved before dispatch.

# Optional: default benchmark config path (overridden by --config CLI flag)
# DEFAULT_CONFIG=../fdp/configs/benchmarks/ga_congress_2026_v2.yml
